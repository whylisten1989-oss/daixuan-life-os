# 设计系统

视觉概念采用“时间脊柱＋行动流＋生活信号带”。桌面以开放时间轴为主，手机重新编排为纵向时间线和底部导航。

- 背景：石墨黑或清洁白，不使用蓝紫霓虹。
- 语义色：玉绿色用于稳定/完成，琥珀色用于专注/提醒，红色只用于风险。
- 容器：优先使用细分隔线、轨道、列表和长数据带；圆角面板只用于需要独立操作边界的区域。
- 字体：中文系统字体与 Geist Sans fallback，数字使用等宽特性。
- 动效：120–260ms 的状态反馈，面板不超过 420ms，减少动画模式关闭非必要位移。

原始概念图位于 `docs/concepts/`，仅作为本项目内部实现规格。
